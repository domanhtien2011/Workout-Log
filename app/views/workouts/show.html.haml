%p= @workout.date
%p= @workout.workout
%p= @workout.mood
%p= @workout.length

#exercises
  %h2 Exercises
  = render @workout.exercises
  %h3 Add an exercise
  = render "exercises/form"


= link_to("Home", root_url)
= link_to("Edit", edit_workout_url)
= link_to("Delete", workout_url, method: :delete, data: { confirm: "Are you sure?" })
